{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["sourceImage", "maskImage", "instruction"],
  "properties": {
    "sourceImage": {
      "type": "string",
      "format": "uuid",
      "title": "原始图片",
      "description": "上传需要局部修改的 PNG、JPG、JPEG 或 WebP 图片。"
    },
    "maskImage": {
      "type": "string",
      "format": "uuid",
      "title": "编辑区域",
      "description": "在原图上涂抹允许模型修改的区域，未涂抹区域会保留原图像素。"
    },
    "instruction": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500,
      "title": "选区修改内容",
      "description": "涂抹区域决定允许修改的范围；这里只描述选区内需要变成什么。"
    }
  },
  "additionalProperties": false
}
